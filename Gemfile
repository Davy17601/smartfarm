# Tooling for SmartFarm — used by the `/skill` commands to register new
# Swift files into the Xcode project without opening Xcode.
#
# One-time setup:  bundle install
# Usage example:   bundle exec ruby -e "require 'xcodeproj'; ..."
source "https://rubygems.org"

gem "xcodeproj", "~> 1.27"
